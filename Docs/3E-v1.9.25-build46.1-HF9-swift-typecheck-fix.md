# 3ERoomElectrical v1.9.25 — Build 46.1-HF9

## Swift compiler type-check hotfix

GitHub Actions failed while type-checking the transform-scoring expression in `RoomProjectGeometryMerger.resolveTransform`.

HF9 preserves the HF8 merge and coordinate-safety behaviour and only rewrites the compiler-heavy collection pipeline as explicit, typed steps:

- validate each candidate transform;
- build transformed wall snapshots in a loop;
- calculate reference-wall distance with explicit `Float` intermediates;
- calculate the score with named values;
- retain the highest-scoring candidate without chained `filter` / `map` / `min` / `max` expressions.

No thresholds, scoring weights, wall matching rules, coordinate transforms, persistence models, UI behaviour, exports, or version numbers changed.

Version remains `1.9.25` and build number remains `46` because this is a compile-only hotfix within Build 46.1.
