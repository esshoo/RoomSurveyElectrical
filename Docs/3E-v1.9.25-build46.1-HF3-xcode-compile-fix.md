# v1.9.25 — Build 46.1-HF3 Xcode Compile Fix

هذا الإصلاح لا يغيّر منطق التعرف الهجين أو بيانات المشروع. وظيفته إغلاق خطأ تجميع إضافي ظهر في GitHub Actions بعد HF2.

## الخطأ المصحح

`RoomCaptureModel.cardinalDirection(_:)`

- الدالة تحسب قيمة `normalizedHeading` أولًا، ثم تستخدم `switch` لإرجاع اسم الاتجاه.
- لأن الدالة تحتوي على تعليمة قبل `switch`، لا يمكن الاعتماد على الإرجاع الضمني لتعبير `switch` في وضع التجميع المستخدم داخل GitHub Actions.
- أضيف `return` صريح لكل فرع: شمال، شمال شرقي، شرق، جنوب شرقي، جنوب، جنوب غربي، غرب، وشمال غربي.

## الحدود

- لا تغيير في إصدار التسويق أو رقم البناء: `1.9.25 (46)`.
- لا تغيير في خوارزمية ARWorldMap أو مطابقة الحائط أو الموقع والبوصلة.
- لا تغيير في نماذج الحفظ أو الترحيل أو حزم `.3eroom`.
- لا تغيير في `DXFExporter`.
- Build 46.2 لم يبدأ.
